#include <windows.h>
#include <stdio.h>

LRESULT CALLBACK MainWin_WndProc(HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam);

LRESULT CALLBACK btn_on_click(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam);
LRESULT CALLBACK B_Color(HWND, UINT, WPARAM, LPARAM);